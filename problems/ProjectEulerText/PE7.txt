import math
primes = [2, 3]
p = 5
while len(primes) <= 10000:
    pScore = 0
    for x in range(2, math.floor(math.sqrt(p))+1):
        if p % x == 0:
            pScore += 1
    if pScore == 0:
        primes.append(p)
    p += 2
print(primes)
print(primes[10000])
